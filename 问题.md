# MythosForge 踩坑与解决方案（备忘 / 面试素材）

本文记录开发与联调中**真实出现过的问题**：现象 → 原因 → 如何验证 → 如何解决 → 面试可讲的要点。新增问题请按文末模板追加，并保持「一类根因一条」，避免重复堆砌日志。

---

## 快速索引

| 主题 | 关键词 | 章节 |
|------|--------|------|
| FastAPI 422，`loc: ["body"]` | 空 body、RequestValidationError | [§1](#1-fastapi-422-loc-body--整条请求体缺失) |
| Java 502，`Bad Gateway` | RestClient、上游错误包装 | [§2](#2-java-502-bad-gateway--writer-返回非-2xx) |
| Spring 502 无详细原因 | `server.error.include-message` | [§3](#3-java-502-响应里没有-message-看不清上游) |
| Python 返回 camelCase / Java 读字段 | Pydantic `serialize_by_alias` | [§4](#4-java-读不到-json-字段或-jpa-奇怪报错) |
| Writer `OPENAI_API_KEY is not set` | `.env` 文件名与路径 | [§5](#5-writer-提示-openai_api_key-is-not-set) |
| uvicorn `Invalid HTTP request received` | JDK HttpURLConnection、分块编码 | [§6](#6-writer-返回-http-400-invalid-http-request-received) |
| Flyway / 表「没生成」 | 连错库、未 clean、迁移文件未提交 | [§7](#7-重启-java-后表未生成) |

---

## 1. FastAPI 422，`loc: ["body"]` — 整条请求体缺失

**现象**

- 日志：`RequestValidationError`，`errors` 里 `loc` 为 `["body"]`（或 `('body',)`），`type: missing`，`input: null`。
- 与「缺少某个字段」不同：缺字段时 `loc` 多为 `["body","targetPlatform"]` 这类。

**原因**

- 路由使用单个 Pydantic 模型形参时，第一层 `'body'` 对应**形参名**，不是 JSON 里的 key。`input: null` 通常表示**没有成功解析出请求体**（常见：**Content-Length 为 0**、或非 JSON / 解析失败）。

**如何验证**

- 对同一 URL 发 **空 body** 的 POST，应复现同类 422。
- 在 FastAPI 侧打 **Content-Length / Content-Type / client**，不读 body（避免影响校验）。本项目：`writer-python/app/main.py` 中间件。

**解决方案**

- 调用方确保 POST **带 UTF-8 JSON body**；Java→Python 路径重点检查 RestClient 是否真写出 body（见 §6）。
- 区分「探针误触」（空 POST）与「业务调用」：对照时间戳与客户端 IP。

**面试怎么说**

- 「422 在 FastAPI 里分两层：路由层请求体缺失 vs 模型字段校验失败，要通过 `loc` 判断；我只靠猜字段名会走错方向。」

---

## 2. Java 502 `Bad Gateway` — Writer 返回非 2xx

**现象**

- 浏览器或前端看到 `502`，`path` 为 `/api/projects/{id}/genre/recommend`。

**原因**

- `GenreService` 捕获 `RestClientResponseException`，将 Writer 的 **4xx/5xx** 包装成 **`HttpStatus.BAD_GATEWAY`**，便于统一表达「上游 Writer 失败」。

**如何验证**

- 看响应里的 **`message`**（需 §3 配置），或 Python 侧 access log 是否在同时间点返回 422/400。

**解决方案**

- 先修 Writer 侧根因；不要在未确认的情况下假定是「数据库」或「前端」单独问题。

**面试怎么说**

- 「网关层把上游错误映射成 502，并在 message 里携带上游状态码和正文片段，缩短排查路径。」

---

## 3. Java 502 响应里没有 `message`，看不清上游

**现象**

- JSON 只有 `timestamp`、`status`、`error`、`path`，没有具体原因。

**原因**

- Spring Boot 默认不一定把 `ResponseStatusException#getReason()` 暴露给 JSON 客户端。

**解决方案**

- `application.yml`：`server.error.include-message: always`（本项目已加）。

**面试怎么说**

- 「可观测性：错误响应要带上对用户/前端有意义的 `message`，否则只知道 502 不知道是哪一段链路。」

---

## 4. Java 读不到 JSON 字段或 JPA 奇怪报错

**现象**

- Python 侧逻辑成功，Java `JsonNode.get("candidateRankings")` 等为 missing，持久化或后续逻辑异常。

**原因**

- Pydantic v2 默认序列化可能用 **Python 字段名（snake_case）**，而 Java/前端约定 **camelCase**。

**解决方案**

- 响应模型使用 `serialize_by_alias=True`（仅响应模型）；请求模型避免无谓的 `serialize_by_alias` 以免个别版本解析异常。
- Java 侧可对关键字段做 **camel/snake 双键兼容**（本项目 `GenreService` 曾加 defensive `pick`）。

**面试怎么说**

- 「全链路字段命名契约要在序列化层显式保证；我会在边界（DTO / OpenAPI）校验，而不是散落在业务里。」

---

## 5. Writer 提示 `OPENAI_API_KEY is not set`

**现象**

- `503` 或启动日志警告。

**原因**

- 密钥写在 **`.env.example`** 或未创建 **`.env`**；或曾经「首次导入配置时无 `.env`」导致行为不符合预期（已通过固定读取 `writer-python/.env` 路径缓解）。

**解决方案**

- 在 **`writer-python/.env`** 配置 `OPENAI_API_KEY` 等；不要用 example 文件代替真实 env。
- 修改 `.env` 后 **重启 uvicorn**；`get_settings()` 若带缓存需留意进程级缓存。

**面试怎么说**

- 「密钥不进仓库；本地用 `.env`，生产用环境变量或密钥管理服务；并区分「模板文件」与「真实配置文件」。」

---

## 6. Writer 返回 HTTP 400 `Invalid HTTP request received`

**现象**

- Java `message`：`writer-python genre/recommend HTTP 400: Invalid HTTP request received.`
- 直连 Python（curl/脚本）正常，经 Java 失败。

**原因**

- 多为 **uvicorn/h11** 认为 HTTP **报文格式不合法**，而非 FastAPI 业务异常。常见于 JDK **`HttpURLConnection`** 发送较大 POST 时 **分块传输 / Content-Length** 与服务器解析不一致。

**解决方案**

- `SimpleClientHttpRequestFactory` **`setBufferRequestBody(true)`**，先缓冲再发，带上稳定 **Content-Length**。
- `postGenreRecommend` 使用 **`json.getBytes(StandardCharsets.UTF_8)`** 作为 body；可加 **`Connection: close`** 减少连接复用边角情况。

**面试怎么说**

- 「跨语言 HTTP 客户端时要警惕协议层差异：业务 JSON 没问题，但传输层非法会导致 400；用缓冲 body + 明确字节编码排查。」

---

## 7. 重启 Java 后「表未生成」

**现象**

- 以为 Flyway 没跑，库里看不到新表。

**原因**

- **连到的 PostgreSQL 不是你以为的那个实例**（本机服务 vs Docker）。
- **`mvn` 未带上新迁移文件**（未 `clean` / 资源未进 `target/classes`）。
- **`flyway_schema_history` 已是高版本**，迁移不会重跑。

**解决方案**

- `\dt` / `flyway_schema_history` 与 JDBC URL **同一库**核对。
- `mvn clean spring-boot:run`；确认 `V2__*.sql` 已提交并在 classpath。
- 详见仓库 [`README.md`](README.md) Day 2 排查小节。

**面试怎么说**

- 「迁移「版本表 + 实际连接串」是两个真理来源；环境漂移（多 Postgres）是最常见乌龙。」

---

## 工具：冒烟脚本

- [`scripts/smoke-genre-recommend.ps1`](scripts/smoke-genre-recommend.ps1)：空 body（422）、直连 Writer、经 Java 转发；502 时配合 `message` 阅读上游错误。

---

## 追加条目模板（复制后填写）

```markdown
### N. 简短标题

**现象**

**原因**

**如何验证**

**解决方案**

**面试怎么说**

```

---

## 维护约定

- 每 **`一类根因` 一条**，同类微调记在「解决方案」小节即可。
- 若与现有条目重复，合并为一条并更新索引表。
- 链接到具体文件路径时使用仓库内相对路径，便于代码跳转。
